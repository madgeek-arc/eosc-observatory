package eu.openaire.observatory.indicator.api;

/**
 * Skeleton to size the endpoint surface — wiring (Spring annotations, request mapping,
 * dependency injection of a catalog lookup) is deferred until persistence is decided.
 *
 * GET /api/v1/indicators        -> IndicatorCatalogResponse
 * GET /api/v1/indicators/{code} -> eu.openaire.observatory.indicator.model.IndicatorDefinition
 */
public class IndicatorCatalogController {
}
