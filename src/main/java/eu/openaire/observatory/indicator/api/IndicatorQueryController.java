package eu.openaire.observatory.indicator.api;

/**
 * Skeleton to size the endpoint surface — wiring (Spring annotations, request mapping,
 * dependency injection of IndicatorQueryService) is deferred until persistence is decided.
 *
 * POST /api/v1/indicator-queries -> eu.openaire.observatory.indicator.result.IndicatorResult
 * body: eu.openaire.observatory.indicator.query.IndicatorQuery
 */
public class IndicatorQueryController {
}
